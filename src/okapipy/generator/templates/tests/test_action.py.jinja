"""Generated by okapipy — tests for the `{{ accessor_chain }}` action.

Exercises the action's verb method(s) against synchronous and asynchronous
clients. HTTP traffic is mocked with `pytest-httpx`. Emitted once and never
overwritten.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Any

import pytest
from pytest_httpx import HTTPXMock

from {{ package }}.client import {{ client_class }}, Async{{ client_class }}


{% if single_op %}
def test_{{ test_attr }}_run_invokes_method(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """`run()` issues a `{{ single_op.method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ single_op.method }}",
        json={"ok": True},
    )
    {% if single_op.has_body %}
    result = client.{{ accessor_chain }}.run({"input": "value"})
    {% else %}
    result = client.{{ accessor_chain }}.run()
    {% endif %}

    assert result is not None
    assert field(result, "ok") is True
    request = httpx_mock.get_request()
    assert request is not None
    assert request.method == "{{ single_op.method }}"


def test_{{ test_attr }}_run_none_on_empty(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
) -> None:
    """`run()` returns `None` when the server replies with an empty body."""
    httpx_mock.add_response(method="{{ single_op.method }}", content=b"")
    {% if single_op.has_body %}
    assert client.{{ accessor_chain }}.run({"input": "value"}) is None
    {% else %}
    assert client.{{ accessor_chain }}.run() is None
    {% endif %}


def test_{{ test_attr }}_run_with_overrides(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
) -> None:
    """`params=...` / `headers=...` overrides on `run()` reach the wire request."""
    httpx_mock.add_response(method="{{ single_op.method }}", json={"ok": True})
    {% if single_op.has_body %}
    client.{{ accessor_chain }}.run(
        {"input": "value"}, params={"trace": "1"}, headers={"X-Trace": "ok"}
    )
    {% else %}
    client.{{ accessor_chain }}.run(
        params={"trace": "1"}, headers={"X-Trace": "ok"}
    )
    {% endif %}

    request = httpx_mock.get_request()
    assert request is not None
    assert request.url.params["trace"] == "1"
    assert request.headers["X-Trace"] == "ok"


@pytest.mark.asyncio
async def test_async_{{ test_attr }}_run_invokes_method(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """Async `run()` issues a `{{ single_op.method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ single_op.method }}",
        json={"ok": True},
    )
    {% if single_op.has_body %}
    result = await async_client.{{ accessor_chain }}.run({"input": "value"})
    {% else %}
    result = await async_client.{{ accessor_chain }}.run()
    {% endif %}

    assert result is not None
    assert field(result, "ok") is True


@pytest.mark.asyncio
async def test_async_{{ test_attr }}_run_none_on_empty(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
) -> None:
    """Async `run()` returns `None` when the server replies with an empty body."""
    httpx_mock.add_response(method="{{ single_op.method }}", content=b"")
    {% if single_op.has_body %}
    assert await async_client.{{ accessor_chain }}.run({"input": "value"}) is None
    {% else %}
    assert await async_client.{{ accessor_chain }}.run() is None
    {% endif %}


@pytest.mark.asyncio
async def test_async_{{ test_attr }}_run_with_overrides(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
) -> None:
    """Async `params=...` / `headers=...` overrides on `run()` reach the wire request."""
    httpx_mock.add_response(method="{{ single_op.method }}", json={"ok": True})
    {% if single_op.has_body %}
    await async_client.{{ accessor_chain }}.run(
        {"input": "value"}, params={"trace": "1"}, headers={"X-Trace": "ok"}
    )
    {% else %}
    await async_client.{{ accessor_chain }}.run(
        params={"trace": "1"}, headers={"X-Trace": "ok"}
    )
    {% endif %}

    request = httpx_mock.get_request()
    assert request is not None
    assert request.url.params["trace"] == "1"
    assert request.headers["X-Trace"] == "ok"
{% else %}
{% for op in operations %}
def test_{{ test_attr }}_{{ op.method | lower }}_invokes_method(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """`{{ op.method | lower }}()` issues a `{{ op.method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ op.method }}",
        json={"ok": True},
    )
    {% if op.has_body %}
    result = client.{{ accessor_chain }}.{{ op.method | lower }}({"input": "value"})
    {% else %}
    result = client.{{ accessor_chain }}.{{ op.method | lower }}()
    {% endif %}

    assert result is not None
    assert field(result, "ok") is True


@pytest.mark.asyncio
async def test_async_{{ test_attr }}_{{ op.method | lower }}_invokes_method(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """Async `{{ op.method | lower }}()` issues a `{{ op.method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ op.method }}",
        json={"ok": True},
    )
    {% if op.has_body %}
    result = await async_client.{{ accessor_chain }}.{{ op.method | lower }}({"input": "value"})
    {% else %}
    result = await async_client.{{ accessor_chain }}.{{ op.method | lower }}()
    {% endif %}

    assert result is not None
    assert field(result, "ok") is True


{% endfor %}
{% endif %}
