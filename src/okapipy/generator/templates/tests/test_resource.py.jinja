"""Generated by okapipy — tests for the `{{ accessor_chain }}` resource.

Exercises CRUD operations defined on the resource against synchronous and
asynchronous clients. HTTP traffic is mocked with `pytest-httpx`. Emitted once
and never overwritten.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Any

import pytest
from pytest_httpx import HTTPXMock

from {{ package }}.client import {{ client_class }}, Async{{ client_class }}


{% if has_retrieve %}
def test_{{ test_attr }}_retrieve_returns_payload(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """`retrieve()` issues a `{{ retrieve_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ retrieve_method }}",
        json={"id": "abc"},
    )
    result = client.{{ accessor_chain }}.retrieve()

    assert result is not None
    assert field(result, "id") == "abc"
{% endif %}


{% if has_update %}
def test_{{ test_attr }}_update_sends_body(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """`update()` issues a `{{ update_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ update_method }}",
        json={"id": "abc"},
    )
    result = client.{{ accessor_chain }}.update({"name": "Acme"})

    assert result is not None
    assert field(result, "id") == "abc"
    request = httpx_mock.get_request()
    assert request is not None
    assert request.method == "{{ update_method }}"
{% endif %}


{% if has_patch %}
def test_{{ test_attr }}_patch_sends_partial_body(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """`patch()` issues a `{{ patch_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ patch_method }}",
        json={"id": "abc"},
    )
    result = client.{{ accessor_chain }}.patch({"name": "Acme"})

    assert result is not None
    assert field(result, "id") == "abc"
{% endif %}


{% if has_delete %}
def test_{{ test_attr }}_delete_returns_none_on_204(
    httpx_mock: HTTPXMock,
    client: {{ client_class }},
) -> None:
    """`delete()` issues a `{{ delete_method }}` and returns `None` on a 204."""
    httpx_mock.add_response(
        method="{{ delete_method }}",
        status_code=204,
    )
    result = client.{{ accessor_chain }}.delete()

    assert result is None
{% endif %}


{% if has_retrieve %}
@pytest.mark.asyncio
async def test_async_{{ test_attr }}_retrieve_returns_payload(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """Async `retrieve()` issues a `{{ retrieve_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ retrieve_method }}",
        json={"id": "abc"},
    )
    result = await async_client.{{ accessor_chain }}.retrieve()

    assert result is not None
    assert field(result, "id") == "abc"
{% endif %}


{% if has_update %}
@pytest.mark.asyncio
async def test_async_{{ test_attr }}_update_sends_body(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """Async `update()` issues a `{{ update_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ update_method }}",
        json={"id": "abc"},
    )
    result = await async_client.{{ accessor_chain }}.update({"name": "Acme"})

    assert result is not None
    assert field(result, "id") == "abc"
{% endif %}


{% if has_patch %}
@pytest.mark.asyncio
async def test_async_{{ test_attr }}_patch_sends_partial_body(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
    field: Callable[[Any, str], Any],
) -> None:
    """Async `patch()` issues a `{{ patch_method }}` and returns the parsed body."""
    httpx_mock.add_response(
        method="{{ patch_method }}",
        json={"id": "abc"},
    )
    result = await async_client.{{ accessor_chain }}.patch({"name": "Acme"})

    assert result is not None
    assert field(result, "id") == "abc"
{% endif %}


{% if has_delete %}
@pytest.mark.asyncio
async def test_async_{{ test_attr }}_delete_returns_none_on_204(
    httpx_mock: HTTPXMock,
    async_client: Async{{ client_class }},
) -> None:
    """Async `delete()` issues a `{{ delete_method }}` and returns `None` on a 204."""
    httpx_mock.add_response(
        method="{{ delete_method }}",
        status_code=204,
    )
    result = await async_client.{{ accessor_chain }}.delete()

    assert result is None
{% endif %}
