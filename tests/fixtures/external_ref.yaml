openapi: 3.0.0
info:
  title: External Refs API
  version: 1.0.0
paths:
  /items:
    get:
      summary: List items
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: 'external_components.yaml#/schemas/Item'
